import React from 'react';
import './Loader.css'; // Import the custom CSS for keyframes and blur effects

const GradientWheel = () => {
  return (
    <div class="loader">
    <span></span>
    <span></span>
    <span></span>
    <span></span>
</div>
  );
};

export default GradientWheel;
